{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f1f3f5;
            color: #343a40;
            font-family: Arial, sans-serif;
        }

        .container {
            padding-left: 15px;
            padding-right: 15px;
        }

        .card {
            border: none;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 1.5rem;
            flex: 1;
        }

        .card-content {
            flex-grow: 1;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .card-text {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .card-price {
            font-size: 1.125rem;
            font-weight: 600;
            color: #007bff;
            margin-bottom: 1.5rem;
        }

        .card-image {
            max-width: 150px;
            height: 180px; /* Adjust this value to make the image longer */
            border-radius: 0.5rem;
            margin: 0 auto;
        }

        .btnMios {
            border-radius: 0.3rem;
            margin-top: auto; /* Ensure button is at the bottom */
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 1.5rem;
        }

        .pagination {
            display: flex;
            align-items: center;
        }

        .pagination a {
            margin: 0 0.5rem;
            font-weight: 600;
            padding: 0.5rem 1rem;
            text-align: center;
            min-width: 100px; /* Set a minimum width for consistency */
        }

        .pagination a.disabled {
            pointer-events: none;
            opacity: 0.6;
        }

        /* Optional: Center the page number text with buttons */
        .pagination p {
            margin: 0 0.5rem;
            padding: 0.5rem 1rem;
            min-width: 100px;
        }

        .pagination a {
            background-color: #28a745; /* Green */
            color: #fff; /* White text */
        }

        .pagination a:hover {
            background-color: #218838; /* Darker green on hover */
        }

        .pagination a.disabled {
            background-color: #c6e3c4; /* Light green for disabled */
            color: #6c757d; /* Secondary Gray text */
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="text-center mb-4">
            <h1 class="display-4">Products</h1>
            <a href="/carts/{{cartId}}" class="btn btn-outline-primary mt-3">View Cart</a>
        </div>

        <div class="row">
            {{#each products}}
            <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex flex-column flex-md-row">
                            <div class="card-content flex-grow-1">
                                <h2 class="card-title">{{title}}</h2>
                                <p class="card-text">{{description}}</p>
                                <p class="card-price">Price: ${{price}}</p>
                            </div>
                            <img src="{{image}}" alt="{{title}}" class="card-image align-self-start">
                        </div>
                        <form class="add-to-cart-form mt-3">
                            <input type="hidden" name="productId" value="{{id}}">
                            <button class="btnMios btn btn-primary w-100" type="submit">Add to cart</button>
                        </form>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        
        <div class="pagination-container">
            <div class="pagination text-center">
                <a href="{{#if hasPrevPage}}/products?page={{prevPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasPrevPage}}disabled{{/unless}}">Previous</a>
                <p class="mt-2">Page {{currentPage}} of {{totalPages}}</p>
                <a href="{{#if hasNextPage}}/products?page={{nextPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasNextPage}}disabled{{/unless}}">Next</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.add-to-cart-form').forEach(form => {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const productId = form.querySelector('input[name="productId"]').value;
                try {
                    const response = await fetch(`/api/carts/{{cartId}}/product/${productId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });
                    if (!response.ok) {
                        throw new Error('Failed to add product to cart');
                    }
                    // Redirect to the cart page
                    window.location.href = `/carts/{{cartId}}`;
                } catch (error) {
                    console.error('Error adding product to cart:', error);
                    // Optionally, show error message to user
                }
            });
        });
    </script>
</body>
</html> --}}

{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f1f3f5;
            color: #343a40;
            font-family: Arial, sans-serif;
        }

        .container {
            padding-left: 15px;
            padding-right: 15px;
        }

        .card {
            border: none;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 1.5rem;
            flex: 1;
        }

        .card-content {
            flex-grow: 1;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .card-text {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .card-price {
            font-size: 1.125rem;
            font-weight: 600;
            color: #007bff;
            margin-bottom: 1.5rem;
        }

        .card-image {
            max-width: 150px;
            height: 180px; /* Adjust this value to make the image longer */
            border-radius: 0.5rem;
            margin: 0 auto;
        }

        .btnMios {
            border-radius: 0.3rem;
            margin-top: auto; /* Ensure button is at the bottom */
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 1.5rem;
        }

        .pagination {
            display: flex;
            align-items: center;
        }
        

        .pagination a {
            margin: 0 0.5rem;
            font-weight: 600;
            padding: 0.5rem 1rem;
            text-align: center;
            min-width: 100px; /* Set a minimum width for consistency */
        }

        .pagination a.disabled {
            pointer-events: none;
            opacity: 0.6;
        }

        /* Optional: Center the page number text with buttons */
        .pagination p {
            margin: 0 0.5rem;
            padding: 0.5rem 1rem;
            min-width: 100px;
        }

        .pagination a {
            background-color: #28a745; /* Green */
            color: #fff; /* White text */
        }

        .pagination a:hover {
            background-color: #218838; /* Darker green on hover */
        }

        .pagination a.disabled {
            background-color: #c6e3c4; /* Light green for disabled */
            color: #6c757d; /* Secondary Gray text */
        }

        /* Media Queries */
        @media (max-width: 992px) { /* Large devices like tablets */
            .card {
                margin-bottom: 1rem;
            }
        }

        @media (max-width: 768px) { /* Medium devices like tablets */
            .card-image {
                max-width: 120px;
                height: auto;
            }
            .pagination-container{margin-bottom: 3000px;}

            .card-title {
                font-size: 1.125rem;
            }

            .card-text {
                font-size: 0.875rem;
            }

            .card-price {
                font-size: 1rem;
            }
        }

        @media (max-width: 576px) { /* Small devices like phones */
            .card-image {
                max-width: 100px;
                height: auto;
            }

            .col-xl-4 .col-lg-4 .col-md-6 .col-sm-12 .mb-4{
                margin-bottom: 4000px;
            }

            .container.m4{
                height:100%;
            }

            .card-title {
                font-size: 1rem;
            }

            .pagination-container{margin-bottom: 3000px;}

            .card-text {
                font-size: 0.75rem;
            }

            .card-price {
                font-size: 0.875rem;
            }

            .pagination-container {
                flex-direction: column;
            }

            .pagination a {
                min-width: auto; /* Adjust for smaller buttons */
            }

            .pagination p {
                margin: 0.5rem 0;
            }
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="text-center mb-4">
            <h1 class="display-4">Products</h1>
            <a href="/carts/{{cartId}}" class="btn btn-outline-primary mt-3">View Cart</a>
        </div>

        <div class="row">
            {{#each products}}
            <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex flex-column flex-md-row">
                            <div class="card-content flex-grow-1">
                                <h2 class="card-title">{{title}}</h2>
                                <p class="card-text">{{description}}</p>
                                <p class="card-price">Price: ${{price}}</p>
                            </div>
                            <img src="{{image}}" alt="{{title}}" class="card-image align-self-start">
                        </div>
                        <form class="add-to-cart-form mt-3">
                            <input type="hidden" name="productId" value="{{id}}">
                            <button class="btnMios btn btn-primary w-100" type="submit">Add to cart</button>
                        </form>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        
        <div class="pagination-container">
            <div class="pagination text-center">
                <a href="{{#if hasPrevPage}}/products?page={{prevPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasPrevPage}}disabled{{/unless}}">Previous</a>
                <p class="mt-2">Page {{currentPage}} of {{totalPages}}</p>
                <a href="{{#if hasNextPage}}/products?page={{nextPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasNextPage}}disabled{{/unless}}">Next</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.add-to-cart-form').forEach(form => {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const productId = form.querySelector('input[name="productId"]').value;
                try {
                    const response = await fetch(`/api/carts/{{cartId}}/product/${productId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });
                    if (!response.ok) {
                        throw new Error('Failed to add product to cart');
                    }
                    // Redirect to the cart page
                    window.location.href = `/carts/{{cartId}}`;
                } catch (error) {
                    console.error('Error adding product to cart:', error);
                    // Optionally, show error message to user
                }
            });
        });
    </script>
</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f1f3f5;
            color: #343a40;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .content {
            flex: 1;
        }

        .container {
            padding-left: 15px;
            padding-right: 15px;
        }

        .card {
            border: none;
            border-radius: 0.5rem;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .card-body {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 1.5rem;
            flex: 1;
        }

        .card-content {
            flex-grow: 1;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .card-text {
            font-size: 1rem;
            color: #6c757d;
            margin-bottom: 1rem;
        }

        .card-price {
            font-size: 1.125rem;
            font-weight: 600;
            color: #007bff;
            margin-bottom: 1.5rem;
        }

        .card-image {
            max-width: 150px;
            height: 180px;
            border-radius: 0.5rem;
            margin: 0 auto;
        }

        .btnMios {
            border-radius: 0.3rem;
            margin-top: auto;
        }

        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 1.5rem;
        }

        .pagination {
            display: flex;
            align-items: center;
        }

        .pagination a {
            margin: 0 0.5rem;
            font-weight: 600;
            padding: 0.5rem 1rem;
            text-align: center;
            min-width: 100px;
        }

        .pagination a.disabled {
            pointer-events: none;
            opacity: 0.6;
        }

        .pagination p {
            margin: 0 0.5rem;
            padding: 0.5rem 1rem;
            min-width: 100px;
        }

        .pagination a {
            background-color: #28a745;
            color: #fff;
        }

        .pagination a:hover {
            background-color: #218838;
        }

        .pagination a.disabled {
            background-color: #c6e3c4;
            color: #6c757d;
        }

        @media (max-width: 992px) {
            .card {
                margin-bottom: 1rem;
            }
        }

        @media (max-width: 768px) {
            .card-image {
                max-width: 120px;
            }

            .card-title {
                font-size: 1.125rem;
            }

            .card-text {
                font-size: 0.875rem;
            }

            .card-price {
                font-size: 1rem;
            }
            .pagination-container {
                
            }
        }

        @media (max-width: 576px) {
            .card-image {
                max-width: 100px;
            }

            .card-title {
                font-size: 1rem;
            }

            .card-text {
                font-size: 0.75rem;
            }

            .card-price {
                font-size: 0.875rem;
            }

            .pagination-container {
                flex-direction: column;
                margin-left: 13%;
            }

            .pagination a {
                min-width: auto;
            }

            .pagination p {
                margin: 0.5rem 0;
            }
        }
    </style>
</head>

<body>
    <div >
        <div class="text-center mb-4">
            <h1 class="display-4">Products</h1>
            <a href="/carts/{{cartId}}" class="btn btn-outline-primary mt-3">View Cart</a>
        </div>

        <div class="row">
            {{#each products}}
            <div class="col-xl-4 col-lg-4 col-md-6 col-sm-12 mb-4">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex flex-column flex-md-row">
                            <div class="card-content flex-grow-1">
                                <h2 class="card-title">{{title}}</h2>
                                <p class="card-text">{{description}}</p>
                                <p class="card-price">Price: ${{price}}</p>
                            </div>
                            <img src="{{image}}" alt="{{title}}" class="card-image align-self-start">
                        </div>
                        <form class="add-to-cart-form mt-3">
                            <input type="hidden" name="productId" value="{{id}}">
                            <button class="btnMios btn btn-primary w-100" type="submit">Add to cart</button>
                        </form>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>

        <div class="pagination-container">
            <div class="pagination text-center">
                <a href="{{#if hasPrevPage}}/products?page={{prevPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasPrevPage}}disabled{{/unless}}">Previous</a>
                <p class="mt-2">Page {{currentPage}} of {{totalPages}}</p>
                <a href="{{#if hasNextPage}}/products?page={{nextPage}}{{else}}#{{/if}}" class="btn btn-secondary {{#unless hasNextPage}}disabled{{/unless}}">Next</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.add-to-cart-form').forEach(form => {
            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                const productId = form.querySelector('input[name="productId"]').value;
                try {
                    const response = await fetch(`/api/carts/{{cartId}}/product/${productId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json'
                        }
                    });
                    if (!response.ok) {
                        throw new Error('Failed to add product to cart');
                    }
                    window.location.href = `/carts/{{cartId}}`;
                } catch (error) {
                    console.error('Error adding product to cart:', error);
                }
            });
        });
    </script>
</body>

</html>

