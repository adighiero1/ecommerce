{{!-- <!-- File: views/uploadDocuments.hbs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Documents</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your stylesheet -->
</head>
<body>
    <div class="container">
        <h1>Upload Documents</h1>
        <form action="/api/users/{{userId}}/documents" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="documents">Select Documents:</label>
                <input type="file" name="documents" id="documents" multiple>
            </div>
            <button type="submit" class="btn btn-primary">Upload</button>
        </form>
        <script>
            console.log("User ID:", "{{userId}}");
        </script>
    </div>
</body>
</html> --}}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Documents</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            max-width: 600px;
            margin-top: 50px;
        }
        .form-group {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="text-center">Upload Documents</h2>
        <form action="/api/users/{{userId}}/documents" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="identificacion">Identificación</label>
                <input type="file" class="form-control" id="identificacion" name="documents" required>
            </div>
            <div class="form-group">
                <label for="comprobanteDomicilio">Comprobante de domicilio</label>
                <input type="file" class="form-control" id="comprobanteDomicilio" name="documents" required>
            </div>
            <div class="form-group">
                <label for="comprobanteEstadoCuenta">Comprobante de estado de cuenta</label>
                <input type="file" class="form-control" id="comprobanteEstadoCuenta" name="documents" required>
            </div>
            <button type="submit" class="btn btn-primary">Upload</button>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

